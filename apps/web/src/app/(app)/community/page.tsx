"use client";

import { FeedPageContainer } from "@/components/containers/community/feed-page-container";

/** /community — Community feed tab. */
const CommunityPage = () => (
  <FeedPageContainer variant="community" group="community" />
);

export default CommunityPage;
